name: second workflow
on: workflow_dispatch

jobs:
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hi
        run: |
          echo "First output"
          echo "Second output"
